dist: bionic

services:
  - docker

stages:
  - lint
  - test

script:
  - docker-compose build

after_script:
  - codecov

jobs:
  include:
    - stage: lint
      install:
        - pip3 install flake8
      before_script: skip
      script:
        - flake8 src
