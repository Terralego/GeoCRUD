django==3.1.*  # Fix with last django available (or LTS)
# django==2.2.*

django-terra-geocrud

# optionnal, if you want to expose API swagger / redoc
drf-yasg
djangorestframework==3.11.*  # drf 12 is not yet supported by drf-yasg

# prod & supervision
gunicorn[gevent]
python-memcached
sentry-sdk
